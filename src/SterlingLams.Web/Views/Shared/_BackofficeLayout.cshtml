@inject SterlingLams.Web.Services.BackofficeChrome Chrome
@{ var backHome = await Chrome.HomeAsync(); }
<!DOCTYPE html>
<html lang="en" class="h-full">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="robots" content="noindex, nofollow" />
    <title>@ViewData["Title"] · Zephiel</title>
    <link rel="stylesheet" href="~/css/app.css" asp-append-version="true" />
    <link rel="icon" type="image/png" sizes="32x32" href="~/favicon-32.png" />
    <partial name="_AccentStyle" />
</head>
<body class="h-full bg-neutral-50 text-neutral-900 antialiased">
    <header class="bg-white border-b border-neutral-200 px-4 md:px-8 py-3 flex items-center gap-3">
        <a href="@backHome"
           class="text-neutral-500 hover:text-neutral-800 text-xs flex items-center gap-1 transition-colors">
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M15 19l-7-7 7-7"/></svg>
            Back to dashboard
        </a>
        <div class="font-cormorant text-lg font-semibold tracking-[0.15em] uppercase mx-auto">Zephiel</div>
        <partial name="_UserChip" />
    </header>

    <main class="max-w-3xl mx-auto px-4 md:px-6 py-8">
        @if (TempData["Success"] != null)
        {
            <div class="mb-5 bg-emerald-50 border border-emerald-200 text-emerald-700 text-sm px-4 py-3 rounded-lg">@TempData["Success"]</div>
        }
        @if (TempData["Error"] != null)
        {
            <div class="mb-5 bg-red-50 border border-red-200 text-red-700 text-sm px-4 py-3 rounded-lg">@TempData["Error"]</div>
        }
        @RenderBody()
    </main>

    <script src="~/js/app.js" asp-append-version="true"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
